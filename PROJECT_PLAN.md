# Qt Video Workbench 完整项目执行计划

项目代号：Qt Video Workbench（暂定，不暗示与 Hypit 官方合作）  
课程展示名称：**基于 Qt 的可编辑视频模板与 Agent 创作工作台**  
版本：1.0｜2026-09-23｜状态：规划已完成，应用尚未开发和联调。

## 1. 要做出的最终产品

用户在 Qt 桌面应用中新建视频工程，选择模板，导入本地素材，通过原生属性控件修改标题、颜色、图片等受支持内容，查看预览并保存可继续编辑的工程。用户可以导出真正的 MP4，看到后台任务状态与成片校验结果。增强版再提供自然语言修改：Agent 给出一项可核对的编辑提案，用户确认后才修改同一个视频工程。

核心不是重写剪映，也不是给网页套一层窗口；本项目自行实现**工程与素材管理、模板实例化、原生属性编辑、版本安全、异步任务管理和交付验收**。Hypit 提供视频语言、编译及执行能力；浏览器预览先复用现有 Studio。[S02][S03]

验收演示主线：新建“校园社团介绍”工程→导入一张本地图片→修改标题与主题色→预览发生变化→撤销一次→导出短片→校验尺寸、时长与可解码性→关闭后重新打开继续编辑。后续可增加“把标题颜色改成橙色”的 Agent 提案，而不是一开始挑战任意视频复刻。

## 2. 已确认条件、默认假设与未知项

已确认：这是 Qt 交互软件课程大作业，截图说明占总评 40%，可使用参赛作品；用户选定 Qt 界面结合 Hypit 的结构化视频工程，并准备交给 Codex 迭代实现。

设计默认：C++17 + Qt 6 Widgets；单人、单机、同一时刻打开一个工程，最多一项活动导出；简体中文 UI；主平台选择开发机上能够获得完整 Qt WebEngine Kit 的桌面系统；应用首版无需生成模型账号。默认优先级是交付可运行软件，而不是追求最多功能。

尚未确认：截止日期、团队人数、教师是否要求纯原生界面、是否强制 C++、最终演示操作系统、已有 Qt Kit、打包是否必须完全便携、可用硬件与下载权限。M0 记录这些问题；能通过环境探测解决的先探测。不能把浏览器 User-Agent 当作最终部署环境。

工时粗估：按 `tasks.json` 当前拆分，P0（M0–M4 加 M6）合计约 85–158 小时，M5 Agent 增强另约 12–21 小时；不含后续 V1.1。每天能稳定投入 4–5 小时时，可暂按约 3–6 周安排 P0，另留机动时间。此处不是工期承诺，实际依赖安装、跨平台及渲染问题可能改变估算；以 M0 结果和每日可投入时间重新排期。

## 3. 产品边界与优先级

| 层级 | 纳入内容 | 完成定义 |
|---|---|---|
| P0 必交闭环 | 工程新建/打开、一个原创模板、本地图片素材、原生属性、预览、单步撤销、真实导出、结果校验、日志与重开工程 | 脱离模型账号仍能完成演示主线 |
| P1 加分能力 | 受约束 Agent、语义时机的解释或调整、两次成片的版本对照、第二模板 | 不损害 P0 稳定性，单独验收 |
| P2 后续探索 | 原生复杂时间线、任意参考视频复刻、多轨实时合成、多模型生成、大批量变体 | 首版不实现 |

首版明确不做：云端账户平台、团队协作、远程服务暴露、微服务、Kubernetes、Kafka、向量数据库、多 Agent 调度、模型训练、任意 Shell Agent、完整 NLE、多平台同时发布。

**MVP 的“AI”不是硬依赖。**Mock 提案只用于测试，必须显示模拟标识；P1 宣称支持真实 Agent 时必须另有真实模型集成证据。

## 4. UI 功能规范

### 4.1 原生工作台

使用 QMainWindow、QSplitter 或 QDockWidget 做可调整布局：左侧为模板/素材及组件列表，中间是浏览器预览，右侧是 Qt 原生属性面板，底部是任务与日志。工具栏包括新建、打开、保存工程、撤销、重做、导出、环境诊断。

每个空状态必须有明确下一步：没有工程提示新建或打开；依赖缺失展示缺什么与检查路径；Source 无法编译显示错误与恢复选项；字段不支持原生编辑时置为只读，不提供无效按钮。

选择模型：P0 使用 Qt 原生组件树选择实体并更新属性面板。嵌入 Studio 中的选择状态与 Qt 不假设自动同步；网页可独立播放和查看。原生选择→网页定位需要后续明确桥接协议，不通过脆弱 DOM 选择器伪造。

### 4.2 工程与素材

新工程从可信模板复制，在用户选择的目录创建项目边界文件、作者源文件、Run、素材目录和工作台元数据。保存工程不触发收费任务、不自动导出。支持路径带空格、中文与 Unicode。

P0 媒体范围先限定为 PNG/JPEG；视频/音频文件可以导入和展示元信息，但未完成对应模板绑定前，不宣称可任意替换成片中的视频/音频。导入不等于素材已经加入画面。只有经过模板 slot 绑定并重新编译才算应用成功。

原文件保持不变；应用复制到工程内、按内容散列命名，使用相对路径。尺寸、媒体类型与解码结果由真实工具/读取结果确认，不信任扩展名。素材大小限制是可配置产品策略，不是安全沙箱。

### 4.3 模板与原生属性

第一个原创模板采用短时长标题卡/图片卡片：标题、副标题、主题色、图片 slot 和简单出现动画。画幅、时长、帧率从模板配置中明确声明，不把某一数值当成 Hypit 默认值。

模板与组件的具体 SVML/Studio Companion 语法必须从固定版本中的示例建立。不要凭计划写出“看起来像 XML”的未验证标签。可先参考官方 chat 示例完成环境贯通，再开发自己的 title-card；两者是不同交付物。[S08][S09]

属性控件由当前 backend snapshot 暴露的可写字段驱动，先支持 text、number、boolean、select、color。list/record、跨多文件属性和未知控件首版只读。不把“组件能渲染”误认为“每个字段都可编辑”。[S06]

### 4.4 预览和导出

预览复用 Studio 的实际组合画面，不把每次修改都当成完整 MP4 导出。QWebEngineView 是否能够在目标机器解码指定视频必须实测，不能只验证网页能加载。[S02][S11]

导出通过真实 Build 和精确 Output 获取，不截屏拼视频，不将示例 MP4 伪装成新结果。活动构建期间按快照记录版本；避免用户改源文件后误认旧任务输出代表最新工程。

### 4.5 Agent 增强

产品内部 Agent 与“用于写项目代码的 Codex”分开。第一版 Agent 是可替换模型接口，输入为用户指令、当前可写字段摘要与工程版本，输出为本项目自定义 EditProposal。

MVP 提案最多一项操作：set_parameter 或受信任模板中的 replace_template_asset。操作必须指向现存 ID，不允许自造路径、Shell、命令行参数或直接生成整份源代码。涉及两项修改时分成两张独立确认卡，不声称跨多个 HTTP 请求具有事务原子性。

## 5. 技术方案

| 部分 | 选择 | 原因/边界 |
|---|---|---|
| 桌面 | C++17 + Qt 6 Widgets | 明确承担课程原生交互能力 |
| 构建 | CMake 3.21+（设计基线） | 小目标分层，Qt Test 与应用分开 |
| 网络 | QNetworkAccessManager | 异步访问当前会话的本地服务 |
| 进程 | QProcess | 管理 Studio 和 CLI 观察进程，不等于拥有共享 Worker |
| 预览 | QWebEngineView | 先用完整 Studio；不假定已有 preview-only 嵌入 API |
| 后端 | 固定版本 Hypit | 不重写编译器和渲染器 |
| 存储 | 源文件 + JSON 元数据/任务记录 + JSONL 日志 | 首版不引入数据库服务 |
| 测试 | Qt Test / CTest + 本地 Mock HTTP/进程 fixture + 真实 E2E | 区分模拟通过与真实可用 |

Qt 最低目标暂设 6.8，M0 以完整 Kit 可用性修订；不是保证所有 >=6.8 版本都兼容同一 Studio。配置文件必须记录实际 Qt、WebEngine、Node、Hypit、FFmpeg 与浏览器版本。

当前核对的 Hypit 开发说明要求 Node >=22.15 与根清单指定的 pnpm；浏览器、媒体程序依真实执行需要准备。以固定版本的 packageManager 和安装说明为准，不把整个 Python/WhisperX 栈作为第一版强制依赖。[S01][S10]

## 6. 代码结构与依赖方向

```text
app/
  main.cpp
  ui/                   MainWindow, TemplatePanel, AssetPanel, InspectorPanel, TaskPanel
  controllers/          ProjectController, EditorController, ExportController, AgentController
  domain/               Project, Snapshot, EditProposal, TaskRecord, ValidationResult
  backend/              IVideoBackend, BackendCapabilities
    hypit/              StudioClient, HypitProcess, HypitCliAdapter, SnapshotMapper
    mock/               MockVideoBackend
  services/             AssetService, ProjectStore, AsyncEditHistory, ValidationService
  infrastructure/       ProcessRunner, HttpClient, LogWriter, PathPolicy, SecretRedactor
  resources/            中文文案、图标（有授权）
templates/              可信原创模板与 manifest
packages/               必要的自定义 Hypit Author / Companion 包
config/                 版本锁与本地配置样例，不含秘密
tests/
  unit/ contract/ integration/ e2e/ fixtures/
docs/                   本包文档与后续运行证据
```

依赖方向为 UI→Controller→Domain/Backend/Service。Hypit 内部字段仅出现在 adapter 层；业务 UI 使用本项目归一化 DTO。先实现一种真实 backend 和一个 Mock，不为了“扩展性”设计插件市场或 RPC 网关。

每次写操作经过同一个 EditorController；工程源文件是权威事实。Snapshot 只是已编译投影，App manifest 不再维护一套独立时间线。详见 `docs/ARCHITECTURE.md`。

## 7. 执行路线与门禁

| 里程碑 | 任务 | 实际交付 | 进入下一步的门禁 |
|---|---|---|---|
| M0 风险验证 | T001–T005 | 环境报告、版本锁、官方本地示例导出、Studio/Qt 探针、真实契约样本 | G0：至少一份真实可解码 MP4、Qt 可显示预览；接口能力清楚 |
| M1 桌面壳与只读连接 | T006–T009 | 可启动 Qt 程序、设置/日志、自动启动 Studio、会话与组件树 | G1：从 Qt 打开工程并读取真实会话，关闭不乱杀进程 |
| M2 工程/素材/原创模板 | T010–T013 | 新建与重开工程、不可变素材导入、原创 title-card 模板 | G2：脱离上游仓库样例目录仍可打开项目并展示素材 |
| M3 原生编辑与历史 | T014–T018 | 动态表单、版本安全写入、异步撤销、受保护源文件修改、外部冲突处理 | G3：Qt 修改真实画面；失败不误报；重开一致 |
| M4 构建与验收 | T019–T023 | plan/build/status/cancel/get 编排、恢复观察、真实导出校验 | G4：完整 P0 创作闭环可重复演示 |
| M5 Agent 增强 | T024–T027 | 提案 Schema、真实模型可选、用户确认、共同执行链、评测语料 | G5：真实/模拟分清；过期、越权、错误提案被拦截 |
| M6 稳定与发布 | T028–T031 | 路径/安全、缓存恢复、主平台包、演示与说明材料 | G6：无开发机路径依赖的主平台发布验证 |

M5 可从正式 P0 交付中裁掉；M6 的安全和发布不可整体裁掉。先做 M0 的真实小闭环，不能在两周 UI 之后才第一次测试视频导出。

建议排期是以门禁而不是日历为准：先获得 G0，再做到 G1/G2；重点投入 M3/M4；最后留出完整测试和发布窗口。所有任务的依赖、文件和测试见 `TASKS.md`。

## 8. 需要提前防止的实现错误

**接口假设。**Studio 使用内部接口，不是已确认稳定的公开 REST SDK。`GET /__studio/session` 的响应本身是 Snapshot，不假定包了一层 `{data:...}`。默认端口不是实际地址发现机制。[S04][S05]

**保存语义。**已观察到参数 mutation 的失败回退，而原文 `PUT /__studio/source` 是另一条写入路径，不能一概承诺自动还原；本项目自行建立单文件预像、重新编译验证和冲突安全恢复。

**成功语义。**HTTP 接受、CLI 退出、Build 完成、Output 获取、成片可解码是不同阶段，任何一层失败不能显示“导出成功”。

**撤销语义。**采用 AsyncEditHistory，只在后端确认成功后推进历史指针。共享 Studio 或外部编辑器改变 Source 后，重新建立历史起点，不拿老快照强行盖回去。

**任务生命周期。**后台执行与终端观察分离；请求取消之后必须查询终态，不把杀掉 observer 当取消。重连只恢复观察；Worker 丢失后不能无条件承诺同一次执行恢复。[S13]

**复用与清理。**跨 Build 复用必须保留明确引用；不擅自删除历史 Result 或 live file。自动清理只做应用缓存，项目资源删除另需引用分析与用户确认。

**嵌入边界。**完整 Studio 可以先作为预览区域；原生控件、播放定位和网页选择是否联动是单独能力。没有已验证桥接就不声称存在。

## 9. 验收与质量目标

功能验收由 `docs/TEST_PLAN.md` 控制。P0 硬门禁包括：新建/打开两条路径、五种基础属性控件的映射单测、原创模板至少文字与颜色的真实修改及图片 slot 应用、撤销、错误 Source 处理、导出与验证、关闭重开、含中文和空格路径、停止观察不误判取消、实际产物存在。单测覆盖五种控件，不等于每个真实模板都必须暴露五种字段；未实际暴露的能力保持禁用。

建议为发布固定一个 8–12 秒的小模板与一种经过验证的 MP4 编码组合。目标分辨率可先用 1280×720，最终由模板与环境报告决定。音轨是否必需必须由模板声明；无声标题动画没有音轨不算失败。

性能只作为待测目标：UI 操作反馈通常不应等待整个网络或渲染任务；预览、冷启动、导出时间分别测量；测量记录机器、依赖版本、输入、样本次数。不得写“秒级启动”“一定实时”或填虚构 P95。

结构验收与内容验收分开：ffprobe 和解码能证明可播放及规格；不能证明标题可读、布局合理或剪辑符合创意。至少人工检查起始、中间、结尾和每个转场；对模板文本和颜色可结合源码/截图证据，不默认引入 OCR。[S16][S17]

## 10. 交付物、来源与后续扩展

正式课程交付包含应用源码、主平台运行包/启动指引、原创模板及授权素材、架构与使用说明、测试报告、一次完整演示视频和第三方依赖说明。工作台原创内容与 Hypit 复用内容在答辩中分别讲清楚。

上游许可证带附加条件，必须按实际版本保留并复核；本计划不保证课程用途天然豁免全部条款，也不将其等同于标准 Apache 2.0。[S14]

P1/P2 的下一步优先做语义时机的可视化解释、自己的 chat-scene 图片消息支持或版本对照；等 P0 有真实完成证据后，再决定是否扩展生成与批量任务。不要先投入意图路由模型和平台级可观测系统。

所有外部事实与核对入口见 `docs/UPSTREAM_EVIDENCE.md`。本计划中未注明上游事实的模块、Schema、目录和验收阈值，均是**为本项目提出的设计**。
